# 📋 KEPCO 개폐기류 시험검사성적서 시스템 — 인수인계서 (Handover)

> 차기 담당 Agent(Claude Code 등)가 이 프로젝트를 바로 이어서 작업할 수 있도록 현재 상태를 정리한 문서입니다. 이전 버전 문서(2026-08-03 오전 작성분)는 실제 코드와 어긋난 부분이 많아 전면 재작성했습니다.

**최종 갱신**: 2026-08-27 (폴더 재정비)

---

## 1. 프로젝트 개요

한국전력공사(KEPCO) 개폐기류 9개 품목에 대한 **납품시험검사 성적서 작성**과 **품질관리 대시보드**를 제공하는 단일 파일(Single-file) HTML 웹앱 모음입니다. 빌드 과정 없이 브라우저에서 바로 열어 쓰는 구조이며, 서버 없이 클라이언트 JS만으로 동작합니다.

가장 중요한 원칙: **구매규격(GS-/ES- 번호 규격서)이 최상위 기준이고, 시험검사 업무절차서는 그 하위 실무 지침**입니다. 규격과 절차서·기존 코드가 다르면 구매규격을 따릅니다. 구매규격 원문 PDF와 이를 정리한 JSON 발췌본이 `관련 규정 및 지침/`에 있습니다.

---

## 2. 폴더 구조 (2026-08-27 재정비)

원칙: **저장소가 추적하는 것은 배포본과 공유 문서뿐**입니다. 나머지(참고자료·테스트 데이터·개발 자산)는 로컬에만 두고 `.gitignore`로 제외합니다. 추적 파일은 12개입니다.

```
260729 Web개발 개폐기 시험검사성적서/
├── index.html                     ← 루트 접속 시 성적서 프로그램으로 리다이렉트
│                                     ※ 배포/ 폴더명을 바꾸면 이 파일의 경로 3곳을 함께 고칠 것
├── assets/kepco-logo.png
│
├── 배포/                          ← 실사용 배포본 (구 '통합패키지/')  [git 추적]
│   ├── 개폐기류 성적서 작성프로그램.html      (메인 앱, ★가장 자주 수정되는 파일)
│   ├── 개폐기류 품질관리 대시보드.html        (QIR XML 일괄 분석 대시보드)
│   ├── 개폐기류 발취점검 수량표(단일규격 자동산출용).html
│   ├── 개폐기류 발취점검 수량표(출력용).html   (A3 인쇄용 매트릭스표, 정적 HTML)
│   └── 개폐기류_시험검사_통합패키지.zip       (위 4개 압축본 — ★손으로 만들지 말 것, 아래 스크립트 사용)
│
├── 문서/                          ← 공유 대상 문서  [git 추적]
│   ├── PROJECT_HANDOVER.md        (본 문서)
│   └── 성적서_프로그램_검토의견.md  (규격 원문 대조 결과, 2026-08-20)
│
├── 개발/                          ← 개발 자산 (배포에 불필요, 대부분 git 제외)
│   ├── 배포zip_만들기.py           [git 추적] 배포 HTML을 고쳤으면 반드시 실행
│   ├── 테스트데이터/               dataset_100_sets/ (200파일) + .zip — 대시보드 검증용
│   ├── 스크립트/                   로고 base64 추출, XML 정규화 테스트 등 1회성 스크립트
│   ├── 원본자산/                   심볼마크.AI [git 추적] + signature_ai/ (서명·도장 .AI 원본)
│   └── 발표자료/                   발표자료.html/.pptx + html_to_pptx.py — 로컬 전용
│
├── 관련 규정 및 지침/              ← 참고자료 (수정 금지, 읽기 전용)
│   ├── 구매규격/                  GS-/ES- 원문 PDF 8종 — ★최상위 근거자료
│   │   └── 폐지/                  대상에서 빠진 규격 보관 (ES-5925-0008)
│   ├── 구매규격 Json 파일/         검수시험 항목 발췌 JSON 8개
│   │                              명명규칙: {규격번호}_{코드}_검수.json
│   ├── 시험성적서 발췌/            품목별 실제 서식 발췌 PDF
│   └── 절차서/                    배전 시험검사 업무절차서.hwp (141MB — README.txt 참고)
│
├── 시험성적서 예시/                ← 한전 공식 엑셀 서식 예시 (.xlsx)
├── 완성품 예시/                    ← 참고자료
├── .venv/                         ← PPT 변환용 가상환경 (개발/발표자료/requirements-pptx.txt로 복원)
└── .gitignore
```

### 배포본을 고쳤을 때

`배포/` 안의 HTML을 수정했으면 **반드시** zip을 다시 만듭니다. 손으로 압축하면 빠뜨립니다 — 실제로 2026-08-19판 zip이 08-27판 프로그램과 8일치 어긋난 채 배포된 적이 있습니다.

```
python 개발/배포zip_만들기.py           # 다시 만들기
python 개발/배포zip_만들기.py --확인     # 최신인지 검사만
```

### 규격 원문이 프로그램과 어긋나는 부분

프로그램은 규격 **10종**을 다루는데 `구매규격/`에는 **8종**만 있습니다.

- `폐지/ES-5925-0008` — 2026-08-06에 대상에서 삭제됨. 현행 대조에 쓰지 말 것
- **EMCB(GS-5925-0012), DEMCB(GS-5925-0141) 원문 없음** — 이 2종은 시험항목·판정기준을 원문과 대조할 방법이 없습니다. PDF 확보가 필요합니다. (`문서/성적서_프로그램_검토의견.md` ④항)

> **주의(2026-08-03 기록, 2026-08-05 갱신)**: 당시 환경(Windows-FUSE 마운트)에서는 `.git`의 `unlink`가 안 되는 문제로 로컬 git이 동작하지 않아 삭제했었습니다. **2026-08-05, 네이티브 Windows 환경(Claude Code)에서 재확인한 결과 이 폴더에서 git이 정상 동작**하여 `git init` 후 `https://github.com/hcna999/Web-Report.git`(main)에 그대로 커밋·푸시를 재개했습니다. 즉 FUSE 문제는 특정 실행 환경(Cowork 샌드박스 등)에만 해당하며, 네이티브 환경에서는 이 폴더에서 바로 `git add`/`commit`/`push`가 가능합니다. (혹시 다시 FUSE류 환경에서 작업하게 되면 이 주의사항이 재발할 수 있으니, 그럴 땐 네이티브 파일시스템에 별도 클론 후 작업하세요.)

---

## 3. 9개 개폐기 규격 (SPECS 배열, 메인 앱 내부)

| id | 구매규격 번호 | 정식 품명 (규격 원문 기준) | 설치형태 |
|---|---|---|---|
| POLE | ES-5925-0004 | 25.8kV 에폭시 주상개폐기 (Eco) | 가공 |
| GIB | ES-5925-0008 | 25.8kV **가스절연 다회로차단기**(지중용) — ⚠️"부하개폐기"아님, 차단기 | 지중 |
| EPOXY | GS-5925-0011 | 25.8kV 에폭시 지상개폐기 | 지상 |
| DRYAIR | GS-5925-0157 | 25.8kV Dry-air 절연 부하개폐기(지중용) | 지중 |
| AIRPAD | GS-5925-0158 | 25.8kV 공기절연 지상개폐기 | 지상 |
| DREC | GS-5925-0139 | 25.8kV 폴리머절연 방향성 Recloser | 가공 |
| PREC | GS-5925-0155 | 25.8kV 폴리머 Recloser(Pulse기능부) | 가공 |
| EFI | GS-5925-0135 | 25.8kV 에폭시 고장구간 차단기(EFI) | 가공 |
| REC | ES-5925-0009 | 25.8kV 폴리머 Recloser | 가공 |

GIB는 "개폐기"가 아니라 "차단기" 계열입니다. 과거 발취점검 수량표 2종 파일 헤더에 "가스절연 부하개폐기"로 잘못 표기돼 있던 것을 정정했습니다 (2026-08-03).

---

## 4. 메인 앱 (`개폐기류 성적서 작성프로그램.html`) 아키텍처

### 4.1 데이터 모델
- `SPECS` 배열: 9개 규격 × steps(그룹) × items(개별 시험항목). 각 item은 `key/name/ref(조항)/crit(판정기준)/sampFn(발취함수)` 기본에, 다음 플래그로 렌더링 종류가 결정됨:
  - `full:true` → 전량검사, 값 입력 없이 일괄 양호/불량
  - `cert:true` → 성적서 대체(별첨 파일 업로드), `cycle`로 발취주기 문구
  - `resistance:'overhead'|'underground'` → 상별/단자쌍별 저항값 실측 그리드
  - `thr:{v,dir}` 또는 `range` → 값 입력 + **자동** 판정(measureAuto)
  - `measureManual:true` → 값 입력 + **수동** 판정 선택(measureManual)
  - 위 아무것도 없으면 기본값 `jdg`: 양호/불량 드롭다운만, **값 입력 불가**
- `S.per(n)`, `S.note1_dry` 등 `S` 객체: 발취수량 계산 함수 모음.
- `STATE`/`CUR`: 현재 선택 규격, 품질등급, 시료별 입력값 런타임 상태.
- `buildQIR()` / `qirToXML()` / `xmlToQIR()`: `kepco-qir-switchgear.v0.2` XML 스키마로 직렬화·역직렬화.

### 4.2 출력 기능
- **PDF 저장** (`btnDownloadPDF` → `downloadPDF(q,'both')`): html2pdf.js(jsPDF) 사용. 저장 컨테이너에 `reportStyles('.print-shell-export')`로 스코프 CSS를 주입해서 라이브 앱 스타일(`.item{border-radius}` 등)이 섞여 표 셀이 깨지는 문제를 막음. 워터마크(한전 로고, opacity 0.25)는 CSS `position:fixed`가 캔버스 래스터라이즈에는 페이지마다 반복 적용되지 않으므로, `pdf.setPage(p)` + `pdf.addImage()`로 매 페이지 수동 스탬핑.
- **XLSX 저장** (`btnPrintLoadedDetail` → `exportToExcel(q)`, **2026-08-05 ExcelJS로 전면 재작성**): 시트 순서 = 갑지 → 을지 → [저항측정 항목별 시트] → [성적서대체 항목별 시트] → 사진대지 → 시료 리스트. `safeSheetName()`으로 31자 제한/중복 시트명 처리.
  - 기존엔 SheetJS(`xlsx@0.18.5`, 무료판)로 값만 나열해 병합 셀·테두리·배경색이 전혀 없었음. SheetJS 무료판은 저장 시 셀 서식을 쓸 수 없는 제약이 있어, 한전 실물 성적서(갑지/을지) 예시와 같은 병합 셀·테두리·헤더 음영을 내려면 **ExcelJS(CDN `exceljs@4.4.0`)로 라이브러리를 교체**함. `<head>`의 SheetJS `<script>` 태그도 함께 교체됨.
  - 공통 팔레트/헬퍼: `XLS_COLOR`, `xlsStyle(cell,opts)`, `xlsJudgeStyle(v)` (PDF `reportStyles()`의 색상 톤과 동일하게 통일 — 제목 `#0d47a1`, 헤더 음영 `#dde9f8`, 라벨 음영 `#eef4fc`, 양호/불량 강조색 등).
  - 갑지: 제목(병합)+메타 5행(검사로트번호/품명·제작회사/규격·시험년월일/수량·시험자/제조번호·시험입회자)+항목 2열 그리드+종합판정 푸터를 실물 예시와 동일한 구조로 병합 셀·테두리 적용.
  - 을지: 제조번호를 열로 갖는 매트릭스(시료 7대 초과 시 자동 landscape), 별첨(cert) 항목은 값 칸을 전체 병합해 텍스트로 표기.
  - 모든 시트에 `pageSetup:{fitToPage:true, fitToWidth:1, fitToHeight:0}` 적용해 인쇄/PDF 변환 시 열이 잘리지 않도록 처리(요청사항: "엑셀하고 PDF 안 깨지게").
  - `exportToExcel()`은 `ExcelJS.Workbook.xlsx.writeBuffer()`(비동기)를 쓰므로 **`async` 함수로 변경**됨. 호출부(`btnExportExcel`/`btnPrintLoadedDetail`의 onclick)는 fire-and-forget으로 그대로 둬도 동작함.
  - 검증: 브라우저에서 실제로 여러 규격(POLE·GIB)·수량(2대/8대)으로 성적서를 채운 뒤 `exportToExcel()` 실행 → 반환된 버퍼를 같은 세션에서 `new ExcelJS.Workbook().xlsx.load()`로 재로드해 시트명/병합 개수/행렬 크기/서식이 기대값과 정확히 일치하는지 확인함(콘솔 에러 없음).
- 버튼 문구는 전부 "출력"이 아니라 "저장"으로 통일(인쇄 대화상자 대신 파일 다운로드).

### 4.3 개선의견 → 구글시트 연동 (2026-08-03 추가, **2026-08-05 URL 반영 완료 ✅**)
- 상단바 **💬 개선의견** 버튼 → 모달(이름/회사명 · 의견, 2026-08-05에 "작성자명"→"이름/회사명"으로 라벨·placeholder 변경해 회사명 입력도 명시적으로 허용) → `fetch(FEEDBACK_WEBAPP_URL, {mode:'no-cors', body: URLSearchParams})`로 Google Apps Script 웹앱에 POST.
- **`FEEDBACK_WEBAPP_URL`에 실제 배포 URL이 반영되어 동작 중**입니다(`https://script.google.com/macros/s/AKfycbz2Qu-.../exec`). 코드 위치: 메인 앱 `$('btnBack').onclick=...` 직후, `// ===== 개선의견 (Google Sheet 연동) =====` 주석 아래.
- Apps Script `doPost`는 시트에 `[timestamp, author, opinion, source]` 4개 필드를 append하는 구조(시트 헤더: 제출일시/작성자명/개선의견/출처). 배포 액세스 권한은 "모든 사용자"로 되어 있어야 익명 제출이 동작함.
- ⚠️ 참고: `mode:'no-cors'`라서 프론트엔드는 실제 응답을 읽지 못하고 성공으로 간주함 → Apps Script 배포가 풀리거나 액세스 권한이 바뀌면 프론트는 "접수되었습니다"라고 뜨는데 실제로는 시트에 안 쌓이는 조용한 실패가 날 수 있음. 주기적으로 시트에 실제로 쌓이는지 확인 필요.
- ⚠️ 이 URL은 인증 없는 공개 엔드포인트이므로, 저장소가 퍼블릭이면 누구나 스팸성 POST를 보낼 수 있음(레포를 private으로 유지하거나 추후 토큰 검증 추가 권장).

---

## 5. 발취점검 수량표 2종

- **`(출력용)`**: 정적 HTML 표, A3 가로 인쇄 전용, 9규격×31행 매트릭스. JS 데이터 구조 없이 `<td>`에 값이 하드코딩되어 있어 규격 변경 시 수기로 셀을 찾아 고쳐야 함.
- **`(단일규격 자동산출용)`**: `FLAT_ITEMS`(37개 항목) + `SPEC_HEADERS` JS 객체 기반. 규격 1개를 선택하면 로트수량·품질등급 입력에 따라 `S_FNS`로 발취수량을 자동 계산. 항목 추가/이름 변경은 `FLAT_ITEMS` 배열과 상단 `TOOLTIP_DATA` 딕셔너리를 함께 수정해야 함(항목명 문자열이 두 곳에서 정확히 일치해야 툴팁이 매칭됨).

이번 세션에서 두 파일 모두 DRYAIR의 PT/O-링/부싱/고체절연물 4개 신규 성적서대체 항목과 GIB 헤더명("다회로차단기") 정정을 반영했습니다.

---

## 6. 이번 세션 주요 변경 이력 (2026-08-03)

1. **PDF 출력 버그 3종 수정**: `reportShell()`의 `${inner}` 누락으로 빈 페이지 나오던 문제, 로고 워터마크 base64가 잘려있던 문제, 표 셀이 알약 모양으로 깨지던 CSS 클래스 충돌 문제.
2. **PDF 저장물에 워터마크·페이지번호 스탬핑, 인쇄→저장 방식 전환**.
3. **구매규격 9종 전수 대조 감사**: `관련 규정 및 지침/구매규격 Json 파일/`의 요약본과 메인 앱 `SPECS` 배열을 1:1 대조.
   - **DRYAIR(GS-5925-0157)**: 성적서 별첨 4항목 누락 발견 → PT시험(7.2.20)/O-링 재질시험(7.2.19)/부싱시험(7.2.30)/고체절연물 물성시험(7.2.38) 추가.
   - **EFI(GS-5925-0135)**: 축전지 성능시험(batt)이 측정값형이 아니라 성적서 대체형(cert)이어야 함 → 수정.
   - **값 입력 누락 5개 유형 점검**: 최소동작/T-C특성, 펄스동작시험, 부분방전시험(PD)에서 `measureManual`/`thr`이 빠져 값을 못 넣던 항목 다수 발견·수정 (GIB의 cb_trip/cb_tc/cb_pd 포함, 발취수량도 규격 기준 매20대로 정정).
   - **GIB 품명 오류**: 발취점검 수량표 2종에서 "가스절연 부하개폐기"로 잘못 표기된 것을 "가스절연 다회로차단기"로 정정.
4. **`dataset_100_sets` 100세트 전체 재생성** (위 SPECS 변경사항 반영 + 기존에 100개 전부 빈 페이지였던 버그도 함께 해결됨).
5. **GitHub 연동**: `https://github.com/hcna999/Web-Report.git`(main)에 최초 커밋 푸시 완료. (단, 이 프로젝트 폴더 자체는 FUSE 마운트라 로컬 git 재사용 불가 — 위 2절 주의사항 참고)
6. **폴더 재정비**: 통합패키지/개발중 2분류로 재구성, 구버전 중복 파일(발취점검 수량표 구버전 2개, Web개발 폴더 내 중복 HTML 4개 + 이전버젼 18개 파일, signature_ai.zip, test 스크래치 폴더, 손상된 .git) 삭제.
7. **구글시트 개선의견 버튼 추가** (5절 참고, URL 설정 필요).

---

## 6-1. 이번 세션 주요 변경 이력 (2026-08-05)

1. **git 환경 재확인**: 네이티브 Windows(Claude Code) 환경에서는 이 폴더 안에서 git이 정상 동작함을 확인 → `git init` 후 기존 `Web-Report` 저장소(main)에 이어서 커밋·푸시 재개 (2절 주의사항 갱신).
2. **개선의견 구글시트 연동 완료**: `FEEDBACK_WEBAPP_URL`에 실제 Apps Script 배포 URL 반영. 모달 라벨을 "작성자명"→"이름/회사명"으로 변경(회사명 입력 명시적 허용).
3. **종합판정 실시간 갱신 버그 수정**: 시험 단계 화면에서 판정 드롭다운을 바꿔도 "종합 판정(자동)" 필드가 다음 페이지로 갔다가 돌아와야 갱신되던 문제 → `updateStepCalc()`에서 `#stepOverallInput` 값을 즉시 다시 계산하도록 수정 (`stepOverall()` 결과를 매 변경마다 반영).
4. **XLSX 저장 기능 전면 재작성 (SheetJS → ExcelJS)**: 4.2절 참고. 실제 한전 성적서 예시 엑셀 4종(`시험성적서 예시/*.xlsx`)을 `openpyxl`로 정밀 분석해 갑지/을지 스타일(병합 셀·테두리·헤더 음영)을 재현. 사용자와 협의해 **범위는 갑지/을지 스타일 통일까지**로 한정(9개 규격별 회로/상 단위 병합 구조 1:1 복제는 범위 밖 — 필요 시 추후 규격별 개별 검증 필요).

---

## 6-2. 웹 게시 및 보안 조치 (2026-08-19)

1. **GitHub Pages 공개 게시**: 저장소 루트에 `index.html`(리다이렉트), `.nojekyll`, `assets/kepco-logo.png` 추가.
   `https://hcna999.github.io/Web-Report/` 접속 시 **성적서 작성 프로그램으로 바로 이동**한다.
   - 앱 본체 사본을 루트에 두지 않고 리다이렉트를 쓴 이유: 원본을 `통합패키지/` 1곳으로 유지하기 위함
     (사본을 두면 통합패키지/·zip·루트 3곳을 수동 동기화해야 함). 앱 파일명/폴더명 변경 시
     `index.html` 안의 경로 3곳(meta refresh / location.replace / 폴백 `<a>`)을 함께 고쳐야 한다.
   - 나머지 3개 도구(대시보드·발취수량표 2종)는 루트에서 링크되지 않지만 직접 URL로는 접근 가능하다.
2. **업체별 품질등급 데이터 제거 (중요)**: 공개 게시로 인해 `MAKERS` 배열의 업체 실명 + 한전 품질등급이
   외부에 노출되는 문제가 있어, **업체별 등급 데이터(`g` 필드, 16개 업체분)를 삭제**했다. 업체명 46건과 순서는 유지.
   - 이제 등급은 자동 채움되지 않고 **검사자가 "품질등급(직접 수정 가능)" 드롭다운에서 직접 선택**한다.
   - `makerGrade()`/`makerHasGrade()`는 호출부 호환을 위해 남아 있으며 항상 `'B'`/`false`를 반환한다.
     (`MAKERS`에 `g`를 다시 넣으면 예전 자동 채움 동작이 그대로 복구됨)
   - 상세화면 제조업체 드롭다운의 `업체명 [등급]` 표기도 제거했다. 등급 데이터가 없는 상태에서 이 표기를 두면
     **전 업체가 일괄 `[B]`로 표시되어 "모든 업체가 B등급"이라는 잘못된 정보**가 되기 때문.
   - 등급 출처 안내 문구를 `기본값(미등록/미평가)` → `기본값 B — 등급을 직접 선택하세요`로 변경(실제와 부합하도록).

> ⚠️ **남은 노출 위험 (사용자 인지 후 현행 유지 결정)**
> - **git 이력**: 위 등급 데이터와 Apps Script URL은 **과거 커밋에 그대로 남아 있다.** 최신 파일에서 지워도
>   `git log`/GitHub에서 열람 가능하므로, 완전히 지우려면 이력 재작성(filter-repo 등)이 필요하고
>   협업자가 있는 상태에서는 강제 푸시가 동반되어 위험하다.
> - **Apps Script URL**: 공개 이력에 남아 있어 인증 없는 스팸 POST가 가능하다. 차단하려면 Apps Script에서
>   **새 배포를 만들어 URL을 교체(rotate)**하고 기존 배포를 보관 취소해야 한다. (2026-08-19 시점 현행 유지 결정)
> - **GitHub Pages는 접근 제어를 지원하지 않는다** (Private Pages는 Enterprise Cloud 전용).
>   HTML에 JS로 비밀번호를 넣는 방식은 소스보기로 뚫리므로 보안이 아니다.
>   로그인을 걸려면 Cloudflare Pages + Cloudflare Access(무료 50명, 이메일 도메인 제한) 등으로 이전해야 한다.

---

## 6-3. 통합패키지 3파일 동기화 점검 (2026-08-19)

성적서 프로그램(SPECS)을 기준으로 나머지 3개 파일을 대조한 결과와 조치.

| 파일 | 상태 | 조치 |
|---|---|---|
| 발취점검 수량표(단일규격) | ✅ 동기화됨 (37항목 · 10규격) | 없음 |
| 발취점검 수량표(출력용) | ⚠️ **6개 항목 누락** | 행 삽입 + 번호 재정렬 |
| 품질관리 대시보드 | ✅ 수정 불필요 | 없음 |

1. **출력용 수량표에 6개 항목이 빠져 있었음** — 성적서 프로그램과 단일규격 수량표에는 있으나 출력용에만 없던 항목:
   PT 50kV 유도내전압시험 / PT 부분방전시험 / 상연결 접속재 시험 / 절연유 PCB 성적서 /
   밀봉재(O-링) 재질시험 / 고체절연물 물성시험. → **31행 → 37행으로 보강**하고 번호를 1~37로 재정렬했다.
   - 값의 출처는 단일규격 수량표의 `FLAT_ITEMS`(성적서와 동기화된 데이터)이며, 삽입 후 6개 행 × 10규격의
     셀 클래스·표시값이 원본과 100% 일치함을 대조 검증했다.
   - 검증 지표: 셀 클래스 분포가 `full 45 / spec 95 / norm 10 / cert 40 / na 180 = 370`으로
     FLAT_ITEMS 정의값과 정확히 일치(37행 × 10규격).
   - 참고: 마지막 행 `기타 부품 성적서`는 원래부터 전 규격 `-`인 빈 행이며, 누락된 6개를 대신 담고 있지 않았다.
2. **A3 인쇄가 1장을 넘던 문제도 함께 수정** — 항목 추가 이전부터 이미 초과 상태였다(31행 기준 336mm > A3 가용 285mm).
   37행이 되며 375.7mm까지 늘어, `@media print`에만 행 높이 압축 규칙을 추가해 **262.5mm(여유 22.5mm)**로 맞췄다.
   화면 표시에는 영향이 없고, 항목명은 7.5pt로 유지해 가독성을 지켰다.
3. **대시보드는 손댈 것이 없었다** — 시험항목을 하드코딩하지 않고 XML을 읽어 집계하는 구조라 항목 변경이 자동 반영된다.
   하드코딩된 문구(등급 배수 `D 3배·C 2배`, 품목 코드 10종 목록)도 현재 규칙과 일치함을 확인했다.

> 💡 **다음에 규격이 바뀌면**: 성적서 `SPECS` → 단일규격 `FLAT_ITEMS` → 출력용 표(행) 순으로 반영해야 하며,
> 출력용은 정적 표라 자동 반영이 안 된다. 두 수량표의 항목 수(37)와 출력용 행 수가 같은지 확인하는 것이
> 가장 빠른 동기화 점검 방법이다.

---

## 7. 다음 담당자를 위한 체크리스트 / 열린 이슈

- [x] `FEEDBACK_WEBAPP_URL`에 실제 Apps Script 배포 URL 반영 완료 (2026-08-05).
- [ ] **(보안)** Apps Script URL이 공개 git 이력에 남아 있음 → 새 배포로 URL 교체 권장 (6-2절 참고, 현재 현행 유지).
- [ ] **(보안)** 업체 등급 데이터가 과거 커밋 이력에 남아 있음 → 완전 삭제하려면 이력 재작성 필요(협업자 있어 신중히).
- [ ] 등급 자동 채움이 필요해지면 `MAKERS`에 `g` 필드를 되살리되, **공개 게시 중에는 노출됨**을 감안할 것.
- [ ] XLSX 내보내기는 현재 **갑지/을지 스타일 통일**까지만 반영됨. 저항측정/전압·전류측정 등 항목별 상세 시트는 회로수·상별 병합 구조를 실물 예시처럼 1:1 재현하지 않음(간단한 표+테두리 수준) — 필요 시 규격별로 개별 작업 필요(공수 큼, 6-1절 참고).
- [ ] `개폐기류 발취점검 수량표(출력용).html`은 정적 HTML이라 향후 규격 변경 시 수기 수정 필요 — 가능하면 `(단일규격 자동산출용)`처럼 데이터 구동 방식으로 통합 리팩터링 검토 여지 있음.
- [ ] 신규 규격 추가 시: `SPECS` 배열에 항목 추가 → 메인 앱에는 자동 반영되지만, 발취점검 수량표 2종과 대시보드는 **수동으로 별도 반영**해야 함(자동 동기화 없음).
- [ ] 구매규격 원문 대조가 필요할 땐 `관련 규정 및 지침/구매규격 Json 파일/*.json`을 먼저 확인 — PDF 원문 발췌·정리가 되어 있어 빠르게 조항 대조 가능.
- [ ] 파일 수정 후에는 반드시 `통합패키지/` 안의 동일 파일도 같이 갱신하고 `개폐기류_시험검사_통합패키지.zip`을 재압축할 것 (배포본과 작업본이 분리되어 있지 않으므로 수동 동기화 필요).

---

**작성자**: Claude (Cowork)
