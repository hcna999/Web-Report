# 📋 KEPCO 개폐기류 시험검사성적서 및 품질관리 시스템 프로젝트 인수인계서 (Handover Document)

> 본 문서는 **Google Antigravity Agent**에서 **Claude** 및 차기 개발 담당자(Agent)로 프로젝트를 원활히 인수인계하기 위해 작성된 종합 가이드입니다. 프로젝트의 구조, 주요 기능, 한전(KEPCO) 규격 기준, 대시보드 구성, PDF/XLSX 출력 엔진, 사용자 주요 요구사항이 상세히 정리되어 있습니다.

---

## 📌 1. 프로젝트 개요 (Project Overview)

* **프로젝트명**: KEPCO 개폐기류 시험검사성적서 작성 프로그램 및 품질관리 대시보드 개발
* **주요 목표**:
  1. 한국전력공사(KEPCO) 개폐기류 구매계약검사(입회/면제검사) 성적서를 표준화된 웹 GUI 환경에서 쉽게 작성·관리.
  2. 한전 공식 엑셀 성적서 양식(갑지/을지)과 100% 동일한 **PDF 출력** 및 **XLSX 엑셀 저장** 지원.
  3. 성적서 대체(공인기관 시험성적서 첨부) 항목의 파일 업로드(이미지/PDF) 및 통합 PDF 인쇄 기능 제공.
  4. QIR XML 표준 데이터 직렬화/파싱 및 100개 시험 데이터셋 자동 생성 테스트 환경 구축.
  5. 작성된 성적서 데이터(XML)를 일괄 분석하는 **실시간 품질관리 대시보드** 연동.

---

## 📁 2. 핵심 파일 및 디렉토리 구조 (Directory Structure)

```
c:\Users\skgkr\Documents\Project\260729 Web개발 개폐기 시험검사성적서\
├── 개폐기류 성적서 작성프로그램.html          # 메인 성적서 작성 애플리케이션 (Core Web App)
├── 개폐기류 품질관리 대시보드.html            # QIR XML 데이터 분석 & 품질 대시보드 (Dashboard)
├── 개폐기류 발취점검 수량표.html             # 수량별 시료 발취 수량 자동 계산표
├── 개폐기류 발취점검 수량표(단일규격 자동산출용).html # 단일규격 전용 수량 계산표
├── 시험성적서 예시/                          # 한전 공식 엑셀 성적서 양식 5종 (.xlsx)
│   ├── 504341_Eco 부하개폐기_09대_선일_260729.xlsx
│   ├── 502712_Dry-Air 절연 부하개폐기_05대_선일_260729.xlsx
│   ├── 504338_에폭시절연 부하개폐기_03대_선일_260729.xlsx
│   ├── 502379_가스절연 리클로저_02대_선일_260729.xlsx
│   └── ...
├── dataset_100_sets/                       # 자동 생성된 100개 세트 (100 XML, 100 HTML - 총 200개 파일)
└── PROJECT_HANDOVER.md                     # 본 인수인계 문서
```

---

## ⚙️ 3. 개폐기 규격 및 한전 검사 기준 (KEPCO Technical Standards)

프로그램 내 `SPECS` 객체에 정의된 9가지 개폐기 규격 및 주요 검사 항목 규칙입니다.

### 9대 주요 개폐기 품목
1. **Eco-부하개폐기 (지상용/지중용)** (`504341` / `504342`) - GS-5925-0004
2. **Dry-Air 절연 부하개폐기 (지상용)** (`502712`) - GS-5925-0003
3. **에폭시절연 부하개폐기 (지중용)** (`504338`) - GS-5925-0002
4. **가스절연 부하개폐기 (지상용/지중용)** (`500351` / `500352`) - GS-5925-0001
5. **가스절연 자동재폐로차단기 (리클로저)** (`502379`) - GS-6110-0001
6. **폴리머절연 자동재폐로차단기 (리클로저)** (`502380`) - GS-6110-0002
7. **Dry-Air 절연 자동재폐로차단기 (리클로저)** (`502381`) - GS-6110-0003

### ⚠️ 필수 규격 반영 사항 (사용자 지침 준수)
* **기밀시험 기준 문구**: SF6 및 Dry-Air 기밀시험의 발취 수량 문구는 반드시 **`5대 미만 전량 (5대 초과시 1대/5대 초과)`**로 수정 적용됨 (미만/초과 구분을 명확히 준수).
* **에폭시절연 부하개폐기(지중용) 부싱 및 접속재시험**: 항목이 빠지지 않도록 표준 시험항목에 포함 완료.
* **용어 표기 엄격성**: 한전 표준용어를 사용하며, `'주석'`과 같은 불필요한 주석성 문구는 포함하지 않음.

---

## 💻 4. 메인 성적서 작성 프로그램 (`개폐기류 성적서 작성프로그램.html`)

### 주요 내부 함수 및 아키텍처
* **`STATE` & `CUR`**: 현재 선택된 규격(`CUR.spec`), 모드(자동/일반), 품질등급(`CUR.grade`), 제조번호 목록, 시험항목별 결과 데이터 관리.
* **`buildQIR()`**: 입력된 시험 데이터를 `kepco-qir-switchgear.v0.2` 표준 XML/JSON 직렬화 데이터 객체로 변환.
* **`gapjiHeaderTable(q)` / `gapjiHTML(q)`**:
  - 갑지(성적서 표지) 생성 엔진.
  - 1행: `검사로트번호 : XXXXXX` 단독 표기.
  - 5행: `제조번호 : 00001 ~ 00009` 시료 범위 자동 출력.
  - 별첨 항목: `"공인기관 성적서 대체, 별첨 (제출일자)"` 표기.
  - 하단: `시험근거`, `품질등급`, `종합판정 (시험합격/불합격)`, `한국전력공사` 서명란 렌더링.
* **`euljiHTML(q)` / `euljiCellVal(it, sn)`**:
  - 을지(개별 시료 결과표) 매트릭스 생성 엔진.
  - 10대 단위 블록 페이징 지원 (2블록 = 20대/페이지).
  - 도금 수치 등 측정값 실측 표기, 별첨(cert) 항목 colspan 행 표기.
* **`exportToExcel(q)`**:
  - SheetJS(`XLSX`) 라이브러리를 사용하여 `갑지`, `을지`, `시료리스트`, `성적서대체_목록` 4개 시트를 한전 공식 예시와 동일한 행·열 및 너비(!cols)로 자동 다운로드.
* **`printReport(q, kind)`**:
  - 브라우저 인쇄 창을 띄워 갑지+을지+첨부 사진/파일을 A4 규격 레이아웃으로 PDF 출력.

---

## 📊 5. 품질관리 대시보드 (`개폐기류 품질관리 대시보드.html`)

QIR XML 성적서 파일들을 드래그 앤 드롭으로 업로드하거나 일괄 파싱하여 품질 모니터링을 수행하는 고성능 단일 파일(Single File) 대시보드입니다.

### 대시보드 핵심 기능
1. **QIR XML 드래그 앤 드롭 파서**:
   - 사용자가 저장한 `.xml` 성적서 파일을 여러 개 한 번에 드래그하거나 `dataset_100_sets` 내의 XML 파일들을 불러와 즉시 실시간 분석.
2. **핵심 품질 지표 (KPI Cards)**:
   - 총 검사 건수 및 총 시료 수량
   - 전체 종합 합격률 (%)
   - 품질 등급별 분포 (S: Excellent, A: Good, B: Normal, C: Limited, D: Poor)
   - 제조회사별 품질 랭킹 및 합격률
3. **품질 동향 및 파레토 분석 차트**:
   - 최근 30일 합격률 추이 그래프 (Line Chart)
   - 결함 원인/불합격 항목 파레토 분석 (Pareto Chart)
   - 품목별/규격별 품질 메트릭스 테이블
4. **자동 인사이트 & 이상 탐지 (Automated Insights)**:
   - 기밀시험, 절연저항, 동작시험 등 불합격빈도가 높은 항목 자동 경고.
   - 품질등급 하락 제조사 알림 및 이상 징후 자동 탐지.
5. **데이터 엑스포트**:
   - 집계된 데이터 통계를 CSV / 엑셀로 내보내기 기능.

---

## 🧪 6. 100개 데이터셋 및 테스트 환경 (`dataset_100_sets`)

* **위치**: `c:\Users\skgkr\Documents\Project\260729 Web개발 개폐기 시험검사성적서\dataset_100_sets\`
* **구성**: Total 200개 파일 (100개 XML 성적서 파일 + 100개 랜더링된 HTML 파일)
* **생성 스크립트**: `scratch/generate_100_datasets_fast.js`
  - Node.js VM Sandbox 환경에서 `개폐기류 성적서 작성프로그램.html`의 9개 규격, 46개 제조사 데이터를 무작위/결함 시나리오로 시뮬레이션하여 자동 생성.
  - 합격률 약 82%, 불합격률 약 18% (다양한 불합격 항목 및 조기 종료 시나리오 포함).

---

## 🎯 7. 사용자 주요 요구사항 및 주의 지침 (User Directives)

1. **정확도 최우선 ("이건 아주 중요한 임무야 정확도가 생명인 작업이니, 검증 확인을 철저히 해줘")**:
   - 코드 수정 후에는 반드시 Node VM syntax 구문 검사 및 스크립트 실행 검증을 거쳐 오류가 없음을 확인해야 함.
2. **출력 버튼 단순화**:
   - `renderFinal()` 내에 `🖨️ PDF 출력`과 `📊 XLSX 출력`, `💾 저장(XML)` 3개의 버튼으로 단순 유지.
3. **한전 공식 예시 엑셀 양식 준수**:
   - `시험성적서 예시/` 폴더 내의 `.xlsx` 파일 구조(검사로트번호 1행 단독, 제조번호 범위 5행, 별첨 텍스트 표기 등)를 그대로 유지.

---

## 🚀 8. 인수인계 후 다음 작업 가이드 (Next Steps for Claude)

1. **대시보드 기능 확장 요청 시**:
   - `개폐기류 품질관리 대시보드.html` 파일 내부의 XML 파서 및 Chart.js / Vanilla JS 렌더링 로직을 참조하여 차트 항목을 확장.
2. **신규 개폐기 규격 추가 요청 시**:
   - `개폐기류 성적서 작성프로그램.html` 내의 `SPECS` 배열에 새 규격 ID, 품명, GS번호, steps(시험항목)를 추가하면 작성 프로그램 및 대시보드에 자동 반영됨.
3. **인쇄/PDF 레이아웃 미세 조정 시**:
   - `reportShell()` 내의 `@page` CSS 및 `.gapji-page`, `.mx` 스타일을 수정.

---

**작성일**: 2026-08-03  
**작성자**: Antigravity AI Pair Programmer
